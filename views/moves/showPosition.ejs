<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Position</title>
</head>
<body>
    <div class="banner">
        <h1>BJJ Position</h1>
    </div>

    <div class="button-container">
        <a href="/positions"><button>Back</button></a>
        <a href="/positions/<%= position._id %>/edit"><button>Edit</button></a>
    </div>

    <div class="delete-button">
        <form action="/positions/<%= position._id %>?_method=delete" method="POST">
        <input type="submit" value="Delete">
        </form>    
    </div>

    <div class="position">
        <p>Position: <%= position.name %></p>
        <img src="<%= position.img %>">    
    </div>

    <div class="learned">
        <p>Learned: <%= position.completed %></p>
    </div>
</body>
</html>