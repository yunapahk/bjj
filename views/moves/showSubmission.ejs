<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Submission</title>
</head>
<body>
    <div class="banner">
        <h1>BJJ Submission</h1>
    </div>

    <div class="button-container">
        <a href="/submissions"><button>Back</button></a>
        <a href="/submissions/<%= submission._id %>/edit"><button>Edit</button></a>
    </div>

    <div class="delete-button">
        <form action="/submissions/<%= submission._id %>?_method=delete" method="POST">
        <input type="submit" value="Delete">
        </form>    
    </div>

    <div class="submission">
        <p>Submission: <%= submission.name %></p>
        <img src="<%= submission.img %>">   
    </div>

    <div class="learned">
        <p>Learned: <%= submission.completed %></p>
    </div>
</body>
</html>