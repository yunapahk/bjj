<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Move</title>
</head>
<body>
    <div class="banner">
        <h1>BJJ Move</h1>
    </div>

    <div class="button-container">
        <a href="/moves"><button>Back</button></a>
        <a href="/moves/<%= move._id %>/edit"><button>Edit</button></a>
    </div>

    <div class="delete-button">
        <form action="/movements/<%= move._id %>?_method=delete" method="POST">
        <input type="submit" value="Delete">
        </form>    
    </div>

    <div class="movement">
        <p>Move: <%= move.name %></p>
        <img src="<%= move.img %>">   
    </div>

    <div class="learned">
        <p>Learned: <%= move.completed %></p>
    </div>
</body>
</html>